Install json-server before launch the project
https://www.npmjs.com/package/json-server

To run project locally need execute next commands:

1.Install packages:
### `npm install`

2.Start JSON server
### `npm run server`

3.Start project
### `npm start`